export declare global {
    namespace ReactNavigation {
        interface RootParamList {
            home: undefined;
            new: undefined;
            details: { orderId: string } //Tipo de parâmetro que será passado para abrir a tela
        }
    }
}